import React from 'react';
import { Box, Typography } from '@mui/material';

const ResponderMap = () => (
  <Box display="flex" alignItems="center" justifyContent="center" height={180} bgcolor="#e3e3e3" borderRadius={2} my={2}>
    <Typography color="text.secondary">[Live Map with Route Placeholder]</Typography>
  </Box>
);

export default ResponderMap;
